const Board = ({ title }) => {
    return (
        <div className="w-1/4 h-full bg-blue-400 rounded-xl">
            <h2 className="py-2 text-center text-white text-xl font-bold">{title}</h2>
        </div>
    );
};

export default Board;
