import CreateButton from "./CreateButton";

const RightColumn = () => {
    return (
        <div className="flex flex-col h-full">
            <CreateButton />
        </div>
    );
};

export default RightColumn;
